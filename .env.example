# API Configuration
# Leave unset to auto-detect based on the current hostname (recommended)
# The app will automatically use <current-hostname>:8000 for API calls
# 
# Set this only if you need to override the API URL:
# VITE_API_URL=http://192.168.1.100:8000  # Use specific IP
# VITE_API_URL=http://localhost:8000      # Force localhost
